# DSA Tracker Frontend (React + Vite)

Environment variable:
- `VITE_API_BASE` (default `http://localhost:5000/api`)

Scripts:
- `npm install`
- `npm run dev`

Pages:
- Login
- Dashboard
- Topics list
- Problems list with checkboxes and links
